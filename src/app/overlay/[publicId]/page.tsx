import type{Metadata}from"next";import{notFound}from"next/navigation";import{OverlayRenderer}from"@/workspace/overlays/renderer/overlay-renderer";import{loadPublicOverlay}from"@/workspace/overlays/renderer/data";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Overlay",robots:{index:false,follow:false,noarchive:true}};export default async function Page({params,searchParams}:{params:Promise<{publicId:string}>;searchParams:Promise<{token?:string}>}){const{publicId}=await params;const token=(await searchParams).token??"";const overlay=await loadPublicOverlay(publicId,token);if(!overlay)notFound();return <main style={{margin:0,width:overlay.config.canvas.width,height:overlay.config.canvas.height,overflow:"hidden",background:"transparent"}}><OverlayRenderer initial={{config:overlay.config,bindings:overlay.bindings,versionNumber:overlay.versionNumber}} publicId={publicId} token={token} refreshSeconds={Number(process.env.OVERLAY_RUNTIME_REFRESH_SECONDS)||5}/></main>}
