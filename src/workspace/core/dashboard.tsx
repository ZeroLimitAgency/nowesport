export function StatCard({ label, value, detail }: { label: string; value: string | number; detail: string }) { return <div className="rounded-xl border border-white/8 bg-white/[.025] p-5"><p className="text-xs uppercase tracking-[.16em] text-white/40">{label}</p><p className="mt-3 text-3xl font-black">{value}</p><p className="mt-1 text-xs text-white/40">{detail}</p></div>; }
export function TrendIndicator({ value }: { value: number }) { return <span className={value >= 0 ? "text-emerald-400" : "text-rose-400"}>{value >= 0 ? "↑" : "↓"} {Math.abs(value)}%</span>; }
export type ChartPoint = { label: string; value: number };
function SimpleChart({ points, mode }: { points: ChartPoint[]; mode: "line" | "area" | "bar" }) { return <div role="img" aria-label={`${mode} chart`} className="flex h-36 items-end gap-2">{points.length ? points.map((point) => <div key={point.label} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t bg-[#e93585]/70" style={{ height: `${Math.max(4, point.value)}%` }} /><span className="text-[10px] text-white/35">{point.label}</span></div>) : <p className="m-auto text-sm text-white/35">Aucune donnée disponible</p>}</div>; }
export const LineChart = ({ points }: { points: ChartPoint[] }) => <SimpleChart points={points} mode="line" />;
export const AreaChart = ({ points }: { points: ChartPoint[] }) => <SimpleChart points={points} mode="area" />;
export const BarChart = ({ points }: { points: ChartPoint[] }) => <SimpleChart points={points} mode="bar" />;
export function ComparisonChart({ series }: { series: { label: string; points: ChartPoint[] }[] }) { return <div>{series.map((item) => <section key={item.label}><h3 className="text-xs text-white/50">{item.label}</h3><LineChart points={item.points} /></section>)}</div>; }
